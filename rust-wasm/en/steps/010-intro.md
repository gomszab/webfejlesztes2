# Rust + WebAssembly

WebAssembly (WASM) is a portable binary instruction format for a stack-based virtual machine.
Rust is an ideal language for targeting WASM because it has no runtime garbage collector.

See also: [Components](./steps/020-components.md)

::include{src="./tables/component-model.md"}
