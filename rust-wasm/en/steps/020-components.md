# WASM Components

The WebAssembly Component Model defines a standard way to compose WASM modules.

::include{src="./snippets/component.wit"}

## Key concepts

- **Interface types** — describe the boundary between components
- **WIT** — WebAssembly Interface Types definition language
- **wasm-bindgen** — Rust tooling to generate JS bindings
